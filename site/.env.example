# Set this only for a confirmed public deployment origin. Without it, builds are noindex
# and generate a blocking robots.txt so staging URLs cannot become canonical.
PUBLIC_SITE_URL=https://www.example.com
